# Dark-Light-Theme-Toggle-JavaScript-Theme-Switcher-
A simple and clean theme switcher that allows users to toggle between dark mode and light mode. The selected theme is saved in localStorage, so the layout stays consistent even after refreshing the page or reopening the browser.
